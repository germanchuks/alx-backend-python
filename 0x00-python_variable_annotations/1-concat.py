#!/usr/bin/env python3
"""Takes two strings as arguments and returns a concatenated string"""


def concat(str1: str, str2: str) -> str:
    """Function that takes string str1 and a string str2 as arguments and
    returns a concatenated string."""
    return str1 + str2
