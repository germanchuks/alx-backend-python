#!/usr/bin/env python3
"""Takes a float as argument and returns the floor."""
import math


def floor(n: float) -> int:
    """Function which takes a float n as argument and returns the floor of the
    float."""
    return math.floor(n)
