#!/usr/bin/env python3
"""Takes a list of floats and returns their sum."""
from typing import List


def sum_list(input_list: List[float]) -> float:
    """Takes list of floats as arguments and returns their sum as a float."""
    return sum(input_list)
